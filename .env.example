PORT=4175
DATABASE_URL=postgres://specship:specship@localhost:5432/specship

# Optional. Dev-only: where the Vite dev server proxies /api requests to.
# Defaults to http://127.0.0.1:4175 (matching PORT above) if not set.
VITE_API_TARGET=http://127.0.0.1:4175
